# Python-Misc

Miscellaneous Python code.

## Folder `db`

Some code to play with PostgreSQL from Docker. To run it:

    docker-compose -f src/db/docker-postges.yml up

To stop:

    docker-compose -f src/db/docker-postges.yml down
